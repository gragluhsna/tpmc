<%= form_for(@producer_lot_detail) do |f| %>
  <% if @producer_lot_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@producer_lot_detail.errors.count, "error") %> prohibited this producer_lot_detail from being saved:</h2>

      <ul>
      <% @producer_lot_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :producer_lot_id %><br />
    <%= f.text_field :producer_lot_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.text_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.text_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
